dependencies:
  idf:
    version: ">=5.1"

  # Ein einzelner WS2812 auf dem Modul. Auf 2.x gepinnt: in 3.x heisst das
  # Feld led_pixel_format anders (color_component_format), das wuerde hier
  # stillschweigend nicht mehr uebersetzen.
  espressif/led_strip: "^2.5"
